fastapi>=0.110
uvicorn[standard]>=0.27
crewai>=1.8.0
# LangChain is no longer a direct dependency because CrewAI’s ``LLM`` class
# handles model connectivity via LiteLLM.  Removing langchain avoids
# conflicts with other packages (e.g. langgraph).  If you need
# LangChain features, install it separately at a compatible version.
sqlalchemy>=2.0
pydantic>=2.1
yfinance>=0.2.36
pandas>=2.2
numpy>=1.26
python-dotenv>=1.0
aiohttp>=3.9